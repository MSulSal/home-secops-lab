# Scripts

Operational scripts for setup, validation, and data export workflows.

Planned scripts:

- `bootstrap.sh`: local environment checks and directory prep
- `validate-pipeline.sh`: verify service health and log flow
- `export-telemetry.sh`: extract analysis-ready datasets for `ml/`

Shell standard: Bash only.
