# Scripts

Operational scripts for setup, validation, and data export workflows.

Planned scripts:

- `bootstrap.ps1`: local environment checks and directory prep
- `validate-pipeline.ps1`: verify service health and log flow
- `export-telemetry.ps1`: extract analysis-ready datasets for `ml/`
